services:
  - type: web
    runtime: static
    name: the-cargo-grid
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist

    routes:
      - type: rewrite
        source: /*
        destination: /index.html